from setuptools import setup, find_packages

setup(
    name='shape_prog',
    version='1.0.0',
    author='Men4il',
    description='Shape tool is implementation of task 3 in computer architecture course',
    packages=find_packages()
)
